apiVersion: v1
kind: Service
metadata:
    name: "kbr"
    labels:
        app: "kbr"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "kbr"